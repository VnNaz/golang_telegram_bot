{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Telegram Bot",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/api",  // ✅ your main.go lives here
            "envFile": "${workspaceFolder}/.env",
            "cwd": "${workspaceFolder}"               // ✅ ensures correct working directory
        }
    ]
}